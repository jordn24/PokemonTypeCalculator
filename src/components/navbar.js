import React from 'react';

const NavBar = () => {
    return (
        <nav className="row mt-3 text-center">
            <span className="display-4 fw-bold text-light">Pokemon Team Builder</span>
        </nav>
    );
};

export default NavBar;